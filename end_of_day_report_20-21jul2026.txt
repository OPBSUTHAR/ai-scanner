END OF DAY — 20-21 Jul 2026
AI Document Scanner (WebEnoid Intern)

BUILT:
- Key manager (Fernet-encrypted) + /api/keys CRUD
- Cloud sync: Google Drive, Dropbox, OneDrive with OAuth + usage tracking
- Persistent config (storage path) + folder browser for custom storage
- Auto-detect endpoint for real-time edge detection
- Tesseract/Google Vision OCR with status indicator

FIXED:
- Duplicate btn-open-cam listeners (two handlers on same button)
- Done button hidden by parent div display:none
- Gallery preview path encoding (backslashes broke onclick on Windows)
- PDF merge used window.open (blocked on mobile) → location.href
- Doc selection used dblclick → single-click for mobile

STRIPPED:
- Removed entire wireless/network/BT/phone relay system
- Deleted phone_camera.html and relay API routes
- Removed 450+ lines of wireless JS. App is self-contained.

REWRITTEN CAMERA:
- Three capture methods: (1) getUserMedia + auto-detect (HTTPS),
  (2) PHONE CAMERA → getUserMedia first, falls back to <input capture>
  (Android native camera; iOS opens gallery — browser limit),
  (3) SELECT FILE for gallery upload
- Camera device selector with external USB camera support
- HTTPS warning banner + isSecureContext() check
- Server auto-detect replaced with client-side pixel stability
  (80x60 thumbnails, brightness + movement, no network round-trip)
- Auto-capture when frame steady for 6 consecutive checks

PHONE ACCESS:
- Camera requires HTTPS. Use --ngrok for instant HTTPS URL.
- Without HTTPS, falls back to <input capture>. This is a browser
  security restriction, not a code bug.

FILES: index.html (~2400), web_app.py (~780), phone_camera.html DELETED,
key_manager.py NEW, cloud_sync.py UPDATED, config/ NEW.

RUN: cd ai_scanner && python -m src.web_app [--ngrok]
