END OF DAY REPORT - 13 Aug 2026
AI Document Scanner (WebEnoid Intern)
App: http://localhost:5000 | Tests: 46/46 pass

PRIOR WORK: per-user cloud sessions (Drive/Dropbox/OneDrive), API keys onboarding, batch flow, quick-login-by-name gate before the dashboard.

NEW TODAY
1. LOGIN / REGISTER ENTRY PAGE (same Archive parchment theme): replaced the bare single-field login with a tabbed card — LOGIN and REGISTER modes. Register is a placeholder (no user store yet) but records identity mode in a new user_mode cookie so the dashboard can tell registered keepers from visitors. Both flows reuse /api/login (mode-aware).
2. GUEST / QUICK LOGIN (per-user random ID): added /api/guest endpoint that mints a unique ID (GUEST-XXXX, 8 chars, ambiguous chars stripped, secrets.choice) per visitor, sets the session + mode cookies, and returns the ID. Frontend reveals the temporary ID on the card before redirecting, with a local JS fallback generator if the server is unreachable. Placeholder for real auth — every person who taps EXPLORE AS GUEST gets their own identity.
3. SIDEBAR IDENTITY: app.js now reads user_mode and labels the sidebar role — GUEST VISITOR // TEMP ID, REGISTERED // ARCHIVE KEEPER, or ARCHIVE KEEPER — and keeps the guest ID as the display name.
4. SESSION COOKIES: /api/logout now clears both user_name and user_mode; /api/session returns {name, mode}.
5. ONEDRIVE CONFIGURED END-TO-END: rewrote OneDrive OAuth — auth URL now redirects back to the app (http://localhost:5000/cloud/callback/onedrive, was a dead :8080), popup auto-closes on success, main window polls status in real time (no paste-code step). Tokens now stored as a persistent MSAL SerializableTokenCache (tokens/<user>.onedrive.json) so refresh tokens survive restarts and access tokens refresh silently past the 1h expiry. /cloud/auth and /cloud/status return the exact redirect URI; Settings shows a guided error if the redirect URI isn't registered. User registered the Azure app (multi-tenant + personal accounts), added keys via Settings API Keys, and connected successfully.
6. .ENV LOADING FIX: the app never loaded .env and _sync_keys_to_env wiped keys not in the UI store. Now load_dotenv() at startup; sync uses precedence UI store > .env > unset, so .env works AND deleting a UI key still reflects UNCONFIGURED.
7. DROPBOX — TRIED REDIRECT, FINAL = PASTE-CODE FLOW: first replaced the paste-auth-code flow (DropboxOAuth2FlowNoRedirect) with a redirect flow (DropboxOAuth2Flow) back to http://localhost:5000/cloud/callback/dropbox. That failed for the user with Dropbox's "Invalid redirect_uri" error because the URI was never registered in the Dropbox app console (Permissions -> Redirect URIs). FINAL (working): restored the OLD paste-code flow — CONNECT opens a popup that shows an auth CODE, the app prompts you to paste it, and /cloud/callback/dropbox?code=... exchanges it (PKCE when no app secret, consumer_secret otherwise, offline refresh tokens preserved). No redirect URI needs to be registered at all. Verified: auth URL has no redirect_uri param; fake-code callback fails gracefully.
8. VIEW FOLDER BUTTON: each connected cloud now shows VIEW FOLDER (new /cloud/folder/<provider> endpoint) opening the AI_Scanner folder in a new tab — Drive folder link, Dropbox /home/AI_Scanner, OneDrive Graph webUrl.
9. REAL-TIME CLOUD USAGE: removed the CHECK CLOUD USAGE button. /cloud/status now returns live usage per connected provider (0 KB shown when empty) rendered inline, and Settings auto-polls it every few seconds.
10. ADMIN-CENTRAL GATE — IMPLEMENTED THEN REVERTED: added an admin-password lock (ADMIN_PASSWORD/.env) over Settings -> API Keys plus /api/admin login/logout/check routes and a signed session cookie so end users couldn't see/edit provider keys (client asked how users would avoid creating their own APIs). Decision reversed the same day — the client chose the centrally-hosted model but the gate added complexity; fully reverted via git revert (commit 3dd9d46) so API Keys behaves exactly as before. Stale/expired Dropbox access token was also purged from the key store and tokens/ so Dropbox reconnects cleanly.
11. PERF / LAG / "TERMINATING" FIXES: the app lagged and stopped responding. Root causes: (a) blocking synchronous Dropbox users_get_current_account() validation in _restore_user and setup_dropbox on an expired token — an 8s+ network hang on every first status/connect; (b) OneDrive MSAL acquire_token_silent network refresh during restore; (c) /cloud/status hit live cloud APIs on every poll; (d) werkzeug dev server was single-threaded so ONE slow call blocked every request. Fixes: removed blocking validations from the restore/connect paths, cached usage stats (30s TTL), and run the server threaded (app.run threaded=True). Verified: simple endpoints ~0.2s, /cloud/status instant on cache hits, no more freeze.

NOT DONE: real user store (password/DB), registration persistence, profile pages.

RUN: .\.venv\Scripts\python -m src.web_app
NEXT: persist registered users (file/DB), add optional password, per-user document folders.
